import React, { Suspense } from "react";
import { defer, useLoaderData, Await } from "react-router";
import { getOffer } from "../Server/api";

export default function OfferDetail() {
    return (
        <h1>OfferDetail page here..</h1>
    )
}